---
import Layout from "../layouts/Layout.astro";
import { Picture } from 'astro:assets';
import { 
  BadgeCheck, ArrowRight, Phone, CircleCheck, ChevronLeft, ChevronRight, 
  Smile, Star, ShieldCheck, Leaf, Clock, Building2, AppWindow, Box, 
  Building, HardHat, Sun, Trees, Snowflake, Quote, HelpCircle, MapPin, 
  ChevronDown, Send, Mail, PawPrint 
} from '@lucide/astro';
import map from "../images/map.png";
import teamImg from "../images/team.png";
import cleaningImg from "../images/cleaning.png";
import kitchenImg from "../images/kitchen.png";
import officeImg from "../images/office.png";
import solarImg from "../images/solar.png";
import windowImg from "../images/window.png";

const heroImages = [
  { src: teamImg, alt: "Unser professionelles Team" },
  { src: kitchenImg, alt: "Gründliche Küchenreinigung" },
  { src: officeImg, alt: "Professionelle Büroreinigung" },
  { src: solarImg, alt: "Schonende PV-Reinigung" },
  { src: windowImg, alt: "Streifenfreie Fensterreinigung" },
];

const BRAND = {
  name: "EMSIG Gebäudedienste GmbH",
  tagline: "Schleswig-Holsteins gründlicher Reinigungsservice",
  phone: "+49 4621 930245",
  email: "info@emsig-gd.de",
  area: "Schleswig-Holstein",
  address: "Husumer Baum 36, 24837 Schleswig",
};

const title = `${BRAND.name} – ${BRAND.tagline}`;
const description = `${BRAND.name}: Unterhalts-, Büro-, Glas-, PV- & Baureinigung in ${BRAND.area}. Schnelle Terminvergabe, versichert & zuverlässig.`;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  name: BRAND.name,
  image: ["/logo.svg"],
  address: {
    "@type": "PostalAddress",
    streetAddress: BRAND.address,
    addressLocality: "Schleswig",
    addressRegion: "SH",
    postalCode: "24837",
    addressCountry: "DE",
  },
  url: "https://emsig.sh/",
  telephone: BRAND.phone,
  areaServed: [
    "Kiel",
    "Lübeck",
    "Flensburg",
    "Neumünster",
    "Norderstedt",
    "Pinneberg",
    "Lübecker Bucht",
    "Kieler Bucht",
    "Schleswig"
  ],
  sameAs: ["#", "#"],
  openingHours: "Mo-Fr 08:00-18:00",
  makesOffer: [
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Unterhaltsreinigung" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Fassadenreinigung" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Glasreinigung" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Baureinigung" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Photovoltaikreinigung" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Gartenpflege" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Fassadenreinigung" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Winterdienst" } },
    { "@type":"Offer", itemOffered:{ "@type":"Service", name:"Entrümpelung" } }
  ]
};

const services = [
  { title: "Unterhaltsreinigung", icon: "building", desc: "Regelmäßig oder einmalig – Büros, Verwaltung, Gewerberäume, Praxen, Küchen und mehr." },
  { title: "Fenster & Glas", icon: "window", desc: "Streifenfrei innen & außen, inkl. Rahmen." },
  { title: "Grundreinigung", icon: "box", desc: "Intensivreinigung inkl. Kühlschrank, Backofen & Fenster." },
  { title: "Fassadenreinigung", icon: "apartment", desc: "Professionelle Reinigung von Hausfassaden & Außenwänden." },
  { title: "Baureinigung", icon: "hardhat", desc: "Grob- und Feinreinigung nach Renovierung/Neubau." },
  { title: "PV-Reinigung", icon: "sun", desc: "Schonende Reinigung Ihrer Photovoltaik-Module." },
  { title: "Gartenpflege", icon: "trees", desc: "Rasen, Hecken, Beete – regelmäßig oder einmalig." },
  { title: "Winterdienst", icon: "snowflake", desc: "Schneeräumen & Streuen nach Satzung." },
];

const iconMap: Record<string, any> = {
  building: Building2,
  window: AppWindow,
  box: Box,
  apartment: Building,
  hardhat: HardHat,
  sun: Sun,
  trees: Trees,
  snowflake: Snowflake,
};

const reviews = [
  { name: "Lena H.", role: "Privathaushalt", text: "Sehr zuverlässig und super Ergebnis. Unsere Fenster haben noch nie so geglänzt!" },
  { name: "Markus K.", role: "Hausbesitzer", text: "Kurzfristig einen Termin bekommen, freundlich & gründlich – absolute Empfehlung." },
  { name: "Office West GmbH", role: "Geschäftskunde", text: "Top Büroreinigung. Transparent, flexibel und pünktlich." },
];

const faqs = [
  { q: "Bringen Sie Material mit?", a: "Ja – gern. Auf Wunsch verwenden wir Ihre bevorzugten Mittel." },
  { q: "Gibt es eine Mindestdauer?", a: "Nein." },
  { q: "Wie läuft die Bezahlung?", a: "Per Rechnung. Geschäftskunden erhalten monatliche Sammelrechnungen." },
  { q: "Sind Sie versichert?", a: "Ja. Schäden sind über unsere Betriebshaftpflicht abgedeckt." },
];
---

<Layout title={title} description={description} jsonLd={jsonLd}>
  <!-- Hero Section -->
  <section class="relative w-full py-12 md:py-20 lg:py-24 bg-gradient-to-br from-blue-50 to-white">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="flex flex-col-reverse lg:flex-row gap-10 items-center">
        <div class="flex flex-col gap-6 lg:w-1/2 items-start text-left">
          <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary/10 text-primary text-xs font-bold uppercase tracking-wide">
            <BadgeCheck class="h-4 w-4" />
            Geprüft & Versichert
          </div>
          <h1 class="text-text-main-light text-4xl md:text-5xl lg:text-6xl font-black leading-[1.1] tracking-tight">
            {BRAND.name}
          </h1>
          <h2 class="text-text-sub-light text-lg md:text-xl font-normal leading-relaxed max-w-[540px]">
            {BRAND.tagline}. Unterhalts-, Büro-, Glas- & Baureinigung in {BRAND.area}. Pünktlich, freundlich und mit Zufriedenheitsversprechen.
          </h2>
          <div class="flex flex-col sm:flex-row w-full max-w-[480px] gap-3 mt-4">
            <a href="#contact" class="flex-grow">
              <button class="w-full h-14 px-8 rounded-lg bg-primary hover:bg-primary-hover text-white text-base font-bold shadow-lg shadow-blue-500/30 transition-all flex items-center justify-center gap-2 whitespace-nowrap">
                <span>Kostenloses Angebot</span>
                <ArrowRight class="h-5 w-5" />
              </button>
            </a>
            <a href={`tel:${BRAND.phone.replace(/\s/g,"")}`}>
              <button class="h-14 px-6 rounded-lg border border-gray-300 hover:bg-gray-50 text-text-main-light text-base font-medium transition-all flex items-center justify-center gap-2 whitespace-nowrap">
                <Phone class="h-5 w-5" />
                <span>{BRAND.phone}</span>
              </button>
            </a>
          </div>
          <div class="flex flex-wrap items-center gap-4 mt-6 text-sm text-text-sub-light">
            <div class="flex items-center gap-1">
              <CircleCheck class="h-[18px] w-[18px] text-green-500" />
              <span>Versichert & zuverlässig</span>
            </div>
            <div class="flex items-center gap-1">
              <CircleCheck class="h-[18px] w-[18px] text-green-500" />
              <span>Flexibel & kurzfristig</span>
            </div>
            <div class="flex items-center gap-1">
              <CircleCheck class="h-[18px] w-[18px] text-green-500" />
              <span>100% Zufriedenheit</span>
            </div>
          </div>
        </div>
        <div class="lg:w-1/2 w-full relative">
          <!-- Hero Carousel -->
          <div class="aspect-[4/3] w-full rounded-2xl overflow-hidden shadow-2xl bg-gray-200 relative" id="hero-carousel">
            {heroImages.map((img, i) => (
              <div 
                class={`carousel-slide absolute inset-0 transition-opacity duration-1000 ${i === 0 ? 'opacity-100' : 'opacity-0'}`}
                data-index={i}
              >
                <Picture 
                  src={img.src} 
                  alt={img.alt} 
                  formats={['avif', 'webp']}
                  class="w-full h-full object-cover"
                  loading={i === 0 ? "eager" : "lazy"}
                />
              </div>
            ))}
            <!-- Carousel Indicators -->
            <div class="absolute bottom-4 left-1/2 -translate-x-1/2 flex gap-2 z-20">
              {heroImages.map((_, i) => (
                <button 
                  class={`carousel-dot w-2.5 h-2.5 rounded-full transition-all duration-300 ${i === 0 ? 'bg-white w-8' : 'bg-white/50 hover:bg-white/75'}`}
                  data-index={i}
                  aria-label={`Bild ${i + 1}`}
                />
              ))}
            </div>
            <!-- Navigation Arrows -->
            <button class="carousel-prev absolute left-3 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/80 hover:bg-white shadow-lg flex items-center justify-center z-20 transition-all opacity-0 group-hover:opacity-100">
              <ChevronLeft class="h-6 w-6 text-text-main-light" />
            </button>
            <button class="carousel-next absolute right-3 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-white/80 hover:bg-white shadow-lg flex items-center justify-center z-20 transition-all opacity-0 group-hover:opacity-100">
              <ChevronRight class="h-6 w-6 text-text-main-light" />
            </button>
          </div>
          <div class="absolute -bottom-6 -left-6 md:bottom-10 md:-left-10 bg-white p-4 rounded-xl shadow-xl z-20 flex items-center gap-3 animate-bounce" style="animation-duration: 3s;">
            <div class="bg-green-100 p-2 rounded-full">
              <Smile class="h-6 w-6 text-green-600" />
            </div>
            <div>
              <p class="font-bold text-sm text-text-main-light">100% Zufriedenheit</p>
              <p class="text-xs text-text-sub-light">Garantierter Service</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Trust Bar -->
  <section class="py-6 bg-surface-light border-y border-gray-200">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-text-sub-light">
        <div class="flex items-center gap-2">
          <Star class="h-5 w-5 text-primary" />
          <span>Volle Kundenzufriedenheit</span>
        </div>
        <div class="flex items-center gap-2">
          <ShieldCheck class="h-5 w-5 text-primary" />
          <span>Geprüft & versichert</span>
        </div>
        <div class="flex items-center gap-2">
          <Leaf class="h-5 w-5 text-primary" />
          <span>Nachhaltige Reinigungsmittel</span>
        </div>
        <div class="flex items-center gap-2">
          <Clock class="h-5 w-5 text-primary" />
          <span>Schnelle Terminvergabe</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="py-16 md:py-24 bg-surface-light" id="services">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
        <div class="max-w-[600px]">
          <h2 class="text-primary text-sm font-bold uppercase tracking-wider mb-2">Unsere Services</h2>
          <h3 class="text-3xl md:text-4xl font-bold text-text-main-light mb-4">Flexibel für Ihre Bedürfnisse</h3>
          <p class="text-text-sub-light text-lg">Wählen Sie, was Sie brauchen – ohne langfristige Bindung. Von der regelmäßigen Reinigung bis zur Spezialreinigung.</p>
        </div>
        <a class="text-primary font-bold flex items-center hover:underline" href="#contact">
          Alle Services anfragen
          <ArrowRight class="h-5 w-5 ml-1" />
        </a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {services.map((service) => {
          const IconComponent = iconMap[service.icon];
          return (
          <div class="group flex flex-col rounded-2xl overflow-hidden bg-white border border-gray-100 shadow-sm hover:shadow-2xl hover:-translate-y-1 transition-all duration-300">
            <div class="p-6 flex flex-col flex-1">
              <div class="flex items-center gap-3 mb-3">
                <div class="w-10 h-10 rounded-full bg-blue-50 flex items-center justify-center text-primary shrink-0">
                  <IconComponent class="h-[22px] w-[22px]" />
                </div>
                <h4 class="text-lg font-bold text-text-main-light">{service.title}</h4>
              </div>
              <p class="text-text-sub-light text-sm leading-relaxed mb-5 flex-1">{service.desc}</p>
              <a href="#contact" class="w-full py-2.5 rounded-lg border border-primary/20 hover:border-primary hover:bg-primary/5 text-primary font-bold text-sm transition-colors flex items-center justify-center gap-2">
                  Angebot anfragen
                </a>
              </div>
          </div>
        )})}
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="py-16 md:py-24 bg-blue-50/50" id="about">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="bg-white rounded-3xl p-6 md:p-12 shadow-sm border border-gray-100">
        <div class="flex flex-col lg:flex-row gap-12 items-center">
          <div class="lg:w-1/2 flex flex-col gap-6">
            <div class="inline-flex items-center gap-2 text-primary font-bold uppercase text-sm tracking-wider">
              <span class="w-8 h-[2px] bg-primary"></span>
              Über EMSIG
            </div>
            <h3 class="text-3xl md:text-4xl font-bold text-text-main-light leading-tight">
              Mehr als nur ein Reinigungsservice. Wir sind Ihr Partner für Sauberkeit.
            </h3>
            <p class="text-text-sub-light text-lg leading-relaxed">
              Unser geprüftes Team sorgt dafür, dass Ihre Räume in sicheren Händen sind. Wir verwenden ausschließlich umweltfreundliche Produkte. Saubere Räume bedeuten ein klares Gefühl – und mehr Zeit für die wichtigen Dinge im Leben.
            </p>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-4">
              <div class="flex items-center gap-3">
                <Leaf class="h-6 w-6 text-primary" />
                <span class="text-text-main-light font-medium">Umweltfreundliche Produkte</span>
              </div>
              <div class="flex items-center gap-3">
                <ShieldCheck class="h-6 w-6 text-primary" />
                <span class="text-text-main-light font-medium">Geprüftes Personal</span>
              </div>
              <div class="flex items-center gap-3">
                <Clock class="h-6 w-6 text-primary" />
                <span class="text-text-main-light font-medium">Flexible Terminplanung</span>
              </div>
              <div class="flex items-center gap-3">
                <PawPrint class="h-6 w-6 text-primary" />
                <span class="text-text-main-light font-medium">Tierfreundlich</span>
              </div>
            </div>
            <a href="#contact" class="mt-4 flex items-center justify-center rounded-lg h-12 px-6 bg-gray-100 hover:bg-gray-200 text-text-main-light text-sm font-bold w-fit transition-colors">
              Jetzt Kontakt aufnehmen
            </a>
          </div>
          <div class="lg:w-1/2 w-full">
            <div class="aspect-[4/3] w-full rounded-2xl overflow-hidden shadow-lg">
              <Picture src={map} formats={['avif', 'webp']} alt="Einsatzgebiet Schleswig-Holstein" class="w-full h-full object-cover" />
            </div>
            <p class="mt-4 text-center text-sm text-text-sub-light">
              Unser Einsatzgebiet: {BRAND.area} (Kiel, Lübeck, Flensburg, Neumünster, u.v.m.)
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Reviews Section (commented out for now)
  <section class="py-16 md:py-24 bg-surface-light" id="reviews">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="text-center max-w-[720px] mx-auto mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-text-main-light mb-4">Was Kund:innen sagen</h2>
        <p class="text-text-sub-light text-lg">Echte Rückmeldungen aus Schleswig-Holstein.</p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {reviews.map((review) => (
          <div class="p-8 rounded-xl bg-background-light flex flex-col gap-4 relative">
            <Quote class="h-12 w-12 text-primary/20 absolute top-6 right-6" />
            <div class="flex gap-1 text-yellow-400">
              {[1,2,3,4,5].map(() => (
                <Star class="h-5 w-5 fill-current" />
              ))}
            </div>
            <p class="text-text-main-light italic">"{review.text}"</p>
            <div class="flex items-center gap-3 mt-auto pt-4 border-t border-gray-200">
              <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center text-primary font-bold">
                {review.name.charAt(0)}
              </div>
              <div>
                <p class="text-sm font-bold text-text-main-light">{review.name}</p>
                <p class="text-xs text-text-sub-light">{review.role}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  -->

  <!-- FAQ Section -->
  <section id="faq" class="py-16 md:py-24 bg-blue-50/50">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="text-center max-w-[720px] mx-auto mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-text-main-light mb-4">Häufige Fragen</h2>
        <p class="text-text-sub-light text-lg">Nicht fündig geworden? Schreiben Sie uns – wir antworten schnell.</p>
      </div>
      <div class="grid md:grid-cols-2 gap-6" itemscope itemtype="https://schema.org/FAQPage">
        {faqs.map((faq) => (
          <div class="rounded-2xl bg-white p-6 shadow-sm border border-gray-100" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
            <h3 class="text-lg font-semibold text-text-main-light flex items-start gap-3" itemprop="name">
              <HelpCircle class="h-6 w-6 text-primary mt-0.5 shrink-0" />
              {faq.q}
            </h3>
            <div itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
              <p class="mt-2 text-text-sub-light pl-9" itemprop="text">{faq.a}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-16 md:py-24 bg-surface-light" id="contact">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="flex flex-col lg:flex-row gap-12 lg:gap-20 items-start">
        <div class="lg:w-5/12 flex flex-col gap-6">
          <div class="inline-flex items-center gap-2 text-primary font-bold uppercase text-sm tracking-wider">
            <span class="w-8 h-[2px] bg-primary"></span>
            Kontakt aufnehmen
          </div>
          <h2 class="text-3xl md:text-4xl font-bold text-text-main-light leading-tight">
            Bereit für strahlende Sauberkeit?
          </h2>
          <p class="text-text-sub-light text-lg leading-relaxed">
            Beschreiben Sie kurz Ihr Objekt und Wunsch-Termin. Wir melden uns schnell mit einem kostenlosen Angebot zurück.
          </p>
          <div class="flex flex-col gap-6 mt-4">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 rounded-xl bg-white flex items-center justify-center text-primary shadow-sm border border-gray-100">
                <Phone class="h-6 w-6" />
              </div>
              <div>
                <p class="text-sm text-text-sub-light font-medium">Telefon</p>
                <a href={`tel:${BRAND.phone.replace(/\s/g,"")}`} class="text-lg font-bold text-text-main-light hover:text-primary">{BRAND.phone}</a>
              </div>
            </div>
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 rounded-xl bg-white flex items-center justify-center text-primary shadow-sm border border-gray-100">
                <Mail class="h-6 w-6" />
              </div>
              <div>
                <p class="text-sm text-text-sub-light font-medium">E-Mail</p>
                <a href={`mailto:${BRAND.email}`} class="text-lg font-bold text-text-main-light hover:text-primary">{BRAND.email}</a>
              </div>
            </div>
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 rounded-xl bg-white flex items-center justify-center text-primary shadow-sm border border-gray-100">
                <MapPin class="h-6 w-6" />
              </div>
              <div>
                <p class="text-sm text-text-sub-light font-medium">Adresse</p>
                <p class="text-lg font-bold text-text-main-light">{BRAND.address}</p>
              </div>
            </div>
          </div>
          <p class="text-xs text-text-sub-light mt-4">Durchschnittliche Antwortzeit <strong>&lt; 60 Min.</strong> (Mo–Fr, 8–18 Uhr).</p>
        </div>
        <div class="lg:w-7/12 w-full bg-white rounded-3xl p-6 md:p-10 shadow-xl shadow-blue-500/5 border border-gray-100">
          <form method="POST" action="https://app.formhive.net/submit/TXrsbVtxGNdNLzQ" class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="col-span-1">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="name">Name *</label>
              <input class="w-full h-12 rounded-lg border border-gray-200 bg-background-light px-4 text-text-main-light placeholder:text-gray-400 focus:border-primary focus:ring-primary focus:outline-none transition-all" id="name" name="name" placeholder="Ihr Name" type="text" required />
            </div>
            <div class="col-span-1">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="email">E-Mail *</label>
              <input class="w-full h-12 rounded-lg border border-gray-200 bg-background-light px-4 text-text-main-light placeholder:text-gray-400 focus:border-primary focus:ring-primary focus:outline-none transition-all" id="email" name="email" placeholder="Ihre E-Mail" type="email" required />
            </div>
            <div class="col-span-1">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="street">Straße und Hausnummer</label>
              <input class="w-full h-12 rounded-lg border border-gray-200 bg-background-light px-4 text-text-main-light placeholder:text-gray-400 focus:border-primary focus:ring-primary focus:outline-none transition-all" id="street" name="street" placeholder="z.B. Musterstraße 12" type="text" />
            </div>
            <div class="col-span-1">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="city">Ort *</label>
              <input class="w-full h-12 rounded-lg border border-gray-200 bg-background-light px-4 text-text-main-light placeholder:text-gray-400 focus:border-primary focus:ring-primary focus:outline-none transition-all" id="city" name="city" placeholder="z.B. Schleswig" type="text" required />
            </div>
            <div class="col-span-1">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="phone">Telefon</label>
              <input class="w-full h-12 rounded-lg border border-gray-200 bg-background-light px-4 text-text-main-light placeholder:text-gray-400 focus:border-primary focus:ring-primary focus:outline-none transition-all" id="phone" name="phone" placeholder="Ihre Telefonnummer" type="tel" />
            </div>
            <div class="col-span-1">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="service">Service</label>
              <div class="relative">
                <select class="w-full h-12 rounded-lg border border-gray-200 bg-background-light px-4 text-text-main-light focus:border-primary focus:ring-primary focus:outline-none transition-all appearance-none cursor-pointer" id="service" name="service">
                  <option value="Unterhaltsreinigung">Unterhaltsreinigung</option>
                  <option value="Fassadenreinigung">Fassadenreinigung</option>
                  <option value="Fenster & Glas">Fenster & Glas</option>
                  <option value="Grund- & Umzug">Grund- & Umzug</option>
                  <option value="Baureinigung">Baureinigung</option>
                  <option value="PV-Reinigung">PV-Reinigung</option>
                  <option value="Gartenpflege">Gartenpflege</option>
                  <option value="Winterdienst">Winterdienst</option>
                </select>
                <ChevronDown class="h-5 w-5 absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 pointer-events-none" />
              </div>
            </div>
            <div class="col-span-1 md:col-span-2">
              <label class="block text-sm font-bold text-text-main-light mb-2" for="message">Nachricht *</label>
              <textarea class="w-full rounded-lg border border-gray-200 bg-background-light p-4 text-text-main-light placeholder:text-gray-400 focus:border-primary focus:ring-primary focus:outline-none transition-all resize-none" id="message" name="message" placeholder="Was soll gereinigt werden? (z.B. 3-Zimmer-Wohnung, Fenster, Umzug)" rows="4" required></textarea>
            </div>
            <div class="col-span-1 md:col-span-2 mt-2">
              <button type="submit" class="w-full h-14 rounded-lg bg-primary hover:bg-primary-hover text-white text-base font-bold shadow-lg shadow-blue-500/30 transition-all flex items-center justify-center gap-2 cursor-pointer">
                <Send class="h-5 w-5" />
                <span>Anfrage senden</span>
              </button>
              <p class="text-xs text-text-sub-light text-center mt-3">Mit dem Absenden stimmen Sie unserer <a href="/datenschutz" class="text-primary hover:underline">Datenschutzerklärung</a> zu.</p>
            </div>
          </form>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Banner -->
  <section class="py-16 md:py-24">
    <div class="max-w-[1280px] mx-auto px-4 md:px-10">
      <div class="rounded-3xl overflow-hidden relative bg-primary flex flex-col md:flex-row items-center">
        <div class="absolute inset-0 opacity-10" style="background-image: radial-gradient(circle at 2px 2px, white 1px, transparent 0); background-size: 24px 24px;"></div>
        <div class="p-10 md:p-20 flex-1 z-10">
          <h2 class="text-white text-3xl md:text-5xl font-black leading-tight mb-6">Sofort starten in {BRAND.area}</h2>
          <p class="text-blue-100 text-lg mb-8 max-w-[500px]">Schicken Sie uns kurz, was gereinigt werden soll. Wir melden uns schnell mit Terminvorschlägen.</p>
          <div class="flex flex-col sm:flex-row gap-4">
            <a href="#contact">
              <button class="px-8 h-12 rounded-lg bg-white text-primary font-bold hover:bg-gray-100 transition-colors shadow-lg">
                Jetzt Angebot sichern
              </button>
            </a>
            <a href={`tel:${BRAND.phone.replace(/\s/g,"")}`} class="flex items-center justify-center px-8 h-12 rounded-lg bg-transparent border-2 border-white text-white font-bold hover:bg-white/10 transition-colors">
              <Phone class="h-5 w-5 mr-2" />
              Jetzt anrufen
            </a>
          </div>
        </div>
        <div class="w-full md:w-1/2 h-64 md:h-auto self-stretch relative z-10 hidden md:block">
          <div class="absolute inset-0 bg-gradient-to-l from-transparent to-primary/80 z-20"></div>
          <Picture 
            src={teamImg} 
            alt="Professionelle Reinigung" 
            formats={['avif', 'webp']}
            class="w-full h-full object-cover"
          />
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Hero Carousel
  document.addEventListener('DOMContentLoaded', () => {
    const carousel = document.getElementById('hero-carousel');
    if (!carousel) return;
    
    const slides = carousel.querySelectorAll('.carousel-slide');
    const dots = carousel.querySelectorAll('.carousel-dot');
    const prevBtn = carousel.querySelector('.carousel-prev');
    const nextBtn = carousel.querySelector('.carousel-next');
    
    let currentIndex = 0;
    let autoplayInterval: ReturnType<typeof setInterval>;
    const AUTOPLAY_DELAY = 5000;
    
    function showSlide(index: number) {
      // Wrap around
      if (index < 0) index = slides.length - 1;
      if (index >= slides.length) index = 0;
      
      // Update slides
      slides.forEach((slide, i) => {
        slide.classList.toggle('opacity-100', i === index);
        slide.classList.toggle('opacity-0', i !== index);
      });
      
      // Update dots
      dots.forEach((dot, i) => {
        dot.classList.toggle('bg-white', i === index);
        dot.classList.toggle('w-8', i === index);
        dot.classList.toggle('bg-white/50', i !== index);
        dot.classList.toggle('w-2.5', i !== index);
      });
      
      currentIndex = index;
    }
    
    function nextSlide() {
      showSlide(currentIndex + 1);
    }
    
    function prevSlide() {
      showSlide(currentIndex - 1);
    }
    
    function startAutoplay() {
      stopAutoplay();
      autoplayInterval = setInterval(nextSlide, AUTOPLAY_DELAY);
    }
    
    function stopAutoplay() {
      if (autoplayInterval) clearInterval(autoplayInterval);
    }
    
    // Event listeners
    prevBtn?.addEventListener('click', () => {
      prevSlide();
      startAutoplay();
    });
    
    nextBtn?.addEventListener('click', () => {
      nextSlide();
      startAutoplay();
    });
    
    dots.forEach((dot, i) => {
      dot.addEventListener('click', () => {
        showSlide(i);
        startAutoplay();
      });
    });
    
    // Pause on hover
    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
    
    // Start autoplay
    startAutoplay();
  });
</script>

<style>
  #hero-carousel:hover .carousel-prev,
  #hero-carousel:hover .carousel-next {
    opacity: 1;
  }
</style>
